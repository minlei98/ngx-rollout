apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: nginx-rollout
spec:
  replicas: 5
  template:
    spec:
      containers:
      - name: nginx
        image: nginx:1.24.0-alpine # Specific image for int
